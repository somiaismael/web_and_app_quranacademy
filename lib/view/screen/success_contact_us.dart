import 'package:flutter/material.dart';

class SuccessContactUs extends StatelessWidget {
  const SuccessContactUs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("success to contact us"),);
  }
}